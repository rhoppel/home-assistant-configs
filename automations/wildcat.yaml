- id: wildcat_node
  alias: 'wildcat_node'
  trigger:
    platform: state
    entity_id: binary_sensor.wildcat_button
    to: 'on'
  condition:
  action:
#    - service: switch.turn_on
#      entity_id: switch.g_d1_relay
#    - delay:
#        milliseconds: 500
    - service: light.turn_off
      entity_id: light.durango
    - service: light.turn_off
      entity_id: light.dakota
    - service: light.turn_off
      entity_id: light.bedroom1
    - service: light.turn_off
      entity_id: light.bedroom2
