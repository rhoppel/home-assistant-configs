# mysql://SERVER_IP/DB_NAME?charset=utf8
db_url: !secret recorder_db_url
purge_interval: 1
purge_keep_days: 3
include:
  domains:
    - switch
    - light
    - sensor
exclude:
  domains:
    - updater
    - automation
    - device_tracker
    - group
    - scene
    - script
